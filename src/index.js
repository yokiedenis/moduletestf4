import React from 'react';
import ReactDOM from 'react-dom/client';

import PincodeLookup from './App.js';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
   <PincodeLookup/>
    </>
);